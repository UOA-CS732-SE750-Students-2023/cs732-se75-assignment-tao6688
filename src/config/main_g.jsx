import React from 'react'
import ReactDOM from 'react-dom/client'
import Grades from '../components/mid/Grades'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Grades />
  </React.StrictMode>
)
